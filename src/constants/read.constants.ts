import type { ReadType } from '../request/models/request-config.model';

export const READ_OBJECT: ReadType = 'object';

export const READ_STRING: ReadType = 'string';

export const READ_FORM_DATA: ReadType = 'formData';

export const READ_BLOB: ReadType = 'blob';

export const READ_ARRAY_BUFFER: ReadType = 'arrayBuffer';

export const READ_AUTO: ReadType = 'auto';
